name: devquiz
description: DevQuiz - KemberllyDEV.

# publish_to: 'none' 

version: 1.0.0+1

environment:
  sdk: '>=2.12.0 <3.0.0'

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.2
  share_plus: ^2.0.1
  google_fonts: ^2.0.0
  # screenshot: ^0.3.0
  screenshot: ^1.0.0-nullsafety.1

dev_dependencies:
  flutter_test:
    sdk: flutter

flutter:
  assets:
    - assets/images/
    - assets/database/

  uses-material-design: true

